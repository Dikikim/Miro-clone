registry=https://registry.npmjs.org/
# vite 8 + @vitejs/plugin-react 4.x have a peer-range mismatch (the plugin only
# lists vite <=7), even though they work together. Allow it so a clean install
# (e.g. Netlify CI) doesn't fail with ERESOLVE.
legacy-peer-deps=true
